spring:
  cloud:
    sentinel:
      transport:
        port: 8719
        dashboard: 192.168.110.20:8333

management:
  endpoints:
    web:
      exposure:
        include: '*'

feign:
  sentinel:
    enabled: true